SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_SCHEMA=public
SUPABASE_TABLE_PREFIX=dl_

# Used by the local in-memory store and by extension development.
# In production, create source_api_tokens rows in Supabase and put the plain
# token only in the extension options or a secure operator setup channel.
EXTENSION_API_TOKEN=
